<configuration name="event_socket.conf" description="Event Socket Library">
  <settings>
    <param name="listen-ip"           value="0.0.0.0"/>
    <param name="listen-port"         value="8021"/>
    <param name="password"            value="pCCuZrc/4wt6JA0IBN/xCM6zdzftcJbi"/>
    <param name="nat-map"             value="false"/>
    <!-- Docker bridge network + loopback only — see esl_trusted in acl.conf.xml.
         The host port is also bound to 127.0.0.1 only in docker-compose.yml, so this
         ACL is defense-in-depth, not the only control. -->
    <param name="apply-inbound-acl"   value="esl_trusted"/>
  </settings>
</configuration>
