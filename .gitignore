## .NET
bin/
obj/
*.user
*.suo
.vs/
_ReSharper*/
*.DotSettings.user

## User secrets (never commit — stored in %APPDATA%\Microsoft\UserSecrets)
**/secrets.json

## Publish output
publish/
out/

## NuGet
*.nupkg
*.snupkg
packages/
!**/packages/repositories.config
.nuget/

## OS
.DS_Store
Thumbs.db
desktop.ini

## IDE
.vscode/
*.swp
*.swo

## Logs
*.log
logs/

## Environment
.env
.env.*

## FreeSWITCH local secrets (copy from vars_local.example.xml)
freeswitch/conf/vars_local.xml

## FreeSWITCH tenant-uploaded audio (runtime assets, not source)
freeswitch/sounds/tenant_*/

## FreeSWITCH call recordings (runtime data, not source — keep the dir, drop the media)
freeswitch/recordings/*
!freeswitch/recordings/.gitkeep

## Local blob storage (screen-capture chunks, merged output — runtime data).
## Root-anchored: a bare "storage/" also matches source dirs like tests/.../Storage/
## (git ignore matching is case-insensitive on this platform).
/storage/
/ContactConnection.Api/storage/

## Docker volumes (local data — not source)
postgres_data/

## Original application reference code — local reference only, never committed
Original Application/
